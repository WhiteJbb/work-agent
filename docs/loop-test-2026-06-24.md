# work-agent 파이프라인 루프 테스트 (2026-06-24)

Vault: `D:\personal-vault`  
LLM: Gemini 2.5 Flash Lite  
테스트 전 상태: 246 tests passing

---

## 실행 흐름

```
capture → distill-today → list-candidates → preview-candidate → promote-candidate → build-context
```

---

## 1. capture

```bash
work-agent capture "source grounding 검증 강화 완료. _spec_from_item에서 \
LLM이 반환한 source_refs 중 실제 노트 경로에 없는 허위 경로를 필터링하고, \
전부 허위일 경우 fallback_refs 전체로 폴백하는 로직 추가. \
E2E smoke test 9개 모두 통과." --project work-agent
```

**결과**
```
capture 생성 완료
  파일: D:\personal-vault\00_Inbox\Captures\20260624-020502-work-agent.md
  vault path: 00_Inbox/Captures/20260624-020502-work-agent.md
```

> frontmatter(project, date, tags) 자동 생성. 경로, 파일명 정상.

---

## 2. distill-today

```bash
work-agent distill-today
```

**결과**
```
distill-today 완료: 후보 7개 생성
  - [knowledge] Source Grounding 검증 및 허위 경로 필터링
  - [knowledge] Distill Agent의 LLMError 처리 및 Source References 필터링
  - [knowledge] Candidate Writer의 Frontmatter에 Summary 항상 포함
  - [knowledge] Distill Candidates의 Source References 규칙 강화
  - [knowledge] E2E Smoke Test Pipeline 및 LLM 실패 방어 로직
  - [knowledge] Post-commit 훅에 LLM 기반 커밋 요약 기능 추가
  - [knowledge] LLM 라우터 기술 부채 해소 및 Fallback Chain 통일
```

> Gemini가 오늘 캡처 노트 1개 + Git summary를 합산해 7개 knowledge 후보 생성.  
> 오늘 작업(source grounding, E2E test, LLM 방어 로직, fallback chain)을 의미 단위로 정확히 분류.

---

## 3. list-candidates

```bash
work-agent list-candidates
```

**결과**
```
후보 11개
  [blog_idea]  git post-commit hook 자동 설치 기능 개발 후기      (06-23)
  [blog_idea]  Git Hook을 활용한 자동 캡처 시스템 구축            (06-23)
  [knowledge] work-agent의 post-commit hook 설치 기능 추가       (06-23)
  [knowledge] git post-commit hook 자동 설치 기능 개요           (06-23)
  [knowledge] Candidate Writer의 Frontmatter에 Summary 항상 포함 (오늘)
  [knowledge] Distill Agent의 LLMError 처리 및 Source Refs 필터링 (오늘)
  [knowledge] Distill Candidates의 Source References 규칙 강화  (오늘)
  [knowledge] E2E Smoke Test Pipeline 및 LLM 실패 방어 로직     (오늘)
  [knowledge] LLM 라우터 기술 부채 해소 및 Fallback Chain 통일   (오늘)
  [knowledge] Post-commit 훅에 LLM 기반 커밋 요약 기능 추가      (오늘)
  [knowledge] Source Grounding 검증 및 허위 경로 필터링          (오늘)
```

> 어제 미승격 후보 4개 + 오늘 생성 7개 = 11개. kind별 분류, 날짜 표시 정상.

---

## 4. preview-candidate

```bash
work-agent preview-candidate \
  "60_Candidates/Knowledge/20260624-020538-source-grounding-검증-및-허위-경로-필터링.md"
```

**frontmatter 확인**
```yaml
type: candidate
candidate_type: knowledge
title: Source Grounding 검증 및 허위 경로 필터링
summary: LLM이 생성한 결과에서 실제 존재하지 않는 소스 경로를 걸러내고, 대체 경로를 사용하는 기법입니다.
status: candidate
source_refs:
  - 10_Worklog/GitSummaries/20260624-020117-work-agent-1532e6bd9c.md
  - 00_Inbox/Captures/20260624-020502-work-agent.md
tags: [llm, source-grounding, verification, error-handling]
```

> `summary` 필드 존재 확인 (candidate_writer 수정 정상).  
> `source_refs`에 실제 노트 경로 2개만 포함 — 허위 경로 없음, source grounding 필터링 정상.  
> body는 `## 개념 / ## 왜 중요한가 / ## 적용 방법` 구조, 200자 이상.

---

## 5. promote-candidate

```bash
work-agent promote-candidate \
  "60_Candidates/Knowledge/20260624-020538-source-grounding-검증-및-허위-경로-필터링.md"
```

**결과**
```
승격 완료
  후보: 60_Candidates/Knowledge/20260624-020538-source-grounding-검증-및-허위-경로-필터링.md
  승격됨: 20_Knowledge/work-agent/20260624-020538-source-grounding-검증-및-허위-경로-필터링.md
  종류: knowledge
```

> `20_Knowledge/work-agent/` 하위 정상 생성.  
> 원본 후보 파일 `status → promoted` 마킹 확인 (smoke test에서도 독립 검증됨).

---

## 6. build-context

```bash
work-agent build-context "source grounding"
```

**결과 (수정 전 source_refs 11개)**
```
40_AgentMemory/00_Profile.md … (×6)
20_Knowledge/work-agent/20260624-...-source-grounding.md    ← ✓ 승격 노트
60_Candidates/Knowledge/20260624-...-source-grounding.md    ← ⚠ 중복 (promote 후 잔류)
10_Worklog/GitSummaries/20260624-...-work-agent.md
60_Candidates/Knowledge/20260624-...-distill-candidates.md  ← ⚠ 미승격 후보
60_Candidates/Knowledge/20260624-...-distill-agent.md       ← ⚠ 미승격 후보
```

**문제**: 승격된 노트와 동일 내용이 `60_Candidates/`에서 중복 검색됨. 검토 전 후보가 `write-blog` · `resume` 등 downstream LLM 작업의 근거로 사용될 수 있어 Candidate→Promote 플로우를 무의미하게 만드는 신뢰 모델 위반.

**수정** (`context_pack_builder.py`):
```diff
- related = self.wiki_service.search(topic, limit=_MAX_NOTES)
- for result in related:
-     note = result.note
+ related = self.wiki_service.search(topic, limit=_MAX_NOTES * 2)
+ for result in related:
+     note = result.note
+     if note.path.startswith("60_Candidates/"): continue
+     if len(note_parts) >= _MAX_NOTES: break
```

수정 후 `60_Candidates/` 경로 완전 제거 확인. 19/19 테스트 통과.

---

## 단계별 평가

| 단계 | 상태 | 비고 |
|------|------|------|
| `capture` | ✅ 정상 | frontmatter · 경로 정확 |
| `distill-today` | ✅ 정상 | Gemini 호출, 7개 후보 정확 분류 |
| `list-candidates` | ✅ 정상 | kind · 날짜 분류 정확 |
| `preview-candidate` | ✅ 정상 | source_refs 필터링 · summary 포함 확인 |
| `promote-candidate` | ✅ 정상 | 20_Knowledge/ 이동 · status 마킹 |
| `build-context` | ✅ 수정됨 | 60_Candidates 제외 패치 후 재검증 통과 |
