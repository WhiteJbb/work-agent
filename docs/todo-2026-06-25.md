# TODO — 2026-06-25

오늘(06-24) 작업 기준으로 내일 이어서 할 것들.

---

## 1. Notion → Obsidian 마이그레이션

**목표**: 오래 남길 가치 있는 문서만 골라 vault 포맷으로 이전.

- [ ] Notion export 다운로드 (Settings → Export → Markdown & CSV)
- [ ] 이전할 카테고리 결정:
  - Knowledge base / 개념 정리 → `20_Knowledge/`
  - 프로젝트 결정 로그 → `30_Projects/*/Decisions/`
  - 작업 일지 → `10_Worklog/`
  - 블로그 초안 → `50_Outputs/Blog/Drafts/`
- [ ] 파일명 정리 스크립트 작성 (Notion export는 UUID 접미사가 붙음)
  - 예: `LLM 파인튜닝 노트 a1b2c3d4.md` → `LLM 파인튜닝 노트.md`
- [ ] frontmatter 추가 (`type`, `status`, `created_at`, `tags`)
- [ ] 이전 후 `work-agent build-context`로 연결 확인

**주의**: 단순 회의록·링크 모음·일회성 메모는 이전하지 않는다.  
Knowledge 선별 기준 (WHY 있음 + 6개월 후 가치 + 재사용 가능) 적용.

---

## 2. 노트북 서버 24시간 자동화

**목표**: 수동 실행 없이 매일 distill이 돌고 결과가 vault에 쌓이게.

- [ ] `distill-today` 스케줄러 설정
  - Linux cron 또는 systemd timer
  - 권장: 매일 23:30 (하루 작업 끝난 후)
  ```bash
  30 23 * * * cd /path/to/work-agent && .venv/bin/work-agent distill-today
  ```
- [ ] `weekly-distill` 스케줄러 설정
  - 매주 일요일 00:00
  ```bash
  0 0 * * 0 cd /path/to/work-agent && .venv/bin/work-agent distill-range --days 7
  ```
- [ ] `capture-session` 자동 트리거 고민
  - 현재: post-commit hook이 git summary를 vault에 저장
  - 추가 고민: IDE 세션 종료 시 자동 capture? tmux hook?
- [ ] Telegram bot 원격 캡처 테스트
  - 모바일에서 아이디어 캡처 → vault 자동 저장 플로우
- [ ] 서버 환경 `.env` 설정 확인 (API 키, vault 경로 등)
- [ ] 스케줄러 실패 시 알림 (cron 메일 or Telegram)

---

## 3. AI 활용도 향상 — Vault 컨텍스트 정비

**현황**: `40_AgentMemory/Core/` 비어있음, `WorkAgent/Context.md` 없음,  
`05_OpenLoops.md` 사실상 빈 파일. AI가 프로젝트 맥락을 모르는 상태.

- [ ] `30_Projects/WorkAgent/Context.md` 작성
  - `90_Templates/ProjectContext.md` 템플릿 사용
  - 오늘 작업 내용 반영: 파이프라인 구조, 핵심 설계 결정, 현재 스택
- [ ] `40_AgentMemory/05_OpenLoops.md` 업데이트
  - 현재 열린 이슈: distill task_type 변경, weekly-distill 미검증 등
- [ ] `AGENTS.md` work-agent CLI 명령어 목록 추가
  - 현재 AGENTS.md는 폴더 권한만 명시, 실제 사용 가능한 도구 안내 없음
- [ ] 세션 시작 시 `build-context` 자동 실행 고민
  - CLAUDE.md에 "작업 전 build-context 먼저 실행" rule 추가?
  - 또는 tmux/IDE hook으로 자동화

---

## 4. work-agent 기술 부채

오늘 작업 중 발견된 것들.

- [ ] **`distill_agent.py` task_type 변경 검토**
  - 현재: `get_task_llm_provider("light", ...)` 사용
  - 문제: distill 프롬프트는 200자+ body, 4개 섹션, wikilink 필수 → `light` 수준이 아님
  - 검토: `"writer"` chain으로 변경 시 Gemini Flash 사용 → 품질↑, 비용↑
  - 판단: Gemini 쿼터 소진 빈도 vs 품질 개선 tradeoff 확인 후 결정

- [ ] **`weekly-distill` E2E 테스트**
  - `distill-range --days 7` 실제 실행해보기 (현재 smoke test만 있음)
  - 출력 품질 확인, 후보 중복 dedup 동작 확인

- [ ] **`suggest-knowledge` / `suggest-blog-topics` 테스트**
  - 오늘 개선된 파이프라인(wikilink 주입, 파일명 개선)과 잘 동작하는지 확인

- [ ] **README 업데이트**
  - 오늘 추가된 기능: `promote-all`, stale 표시, `_inject_related_links`
  - Done/Partial/TODO 상태 표 갱신

- [ ] **`capture` 파일명 통일 고민**
  - 현재 raw capture는 여전히 `20260624-031239-work-agent.md` 형식
  - 제목이 없는 captures는 타임스탬프가 맞을 수 있음 (의도적 raw)
  - 명시적 `--title` 옵션 줬을 때만 제목 기반으로 변경?

---

## 5. Vault 후처리

- [ ] Decisions 2개 승격 (`work-agent promote-all --kind decision`)
  - `60_Candidates 디렉토리를 검토 전 임시 영역으로 취급 및 검색 제외`
  - `knowledge 후보 선별 기준 명확화 및 할루시네이션 방지`
- [ ] BlogIdeas 3개 중 1개 골라 초안 작성 시작
  - `LLM으로 커밋 요약 자동화: 개발 생산성 향상의 비결` — 오늘 직접 구현한 내용이라 소재 풍부

---

## 우선순위 제안

| 순서 | 항목 | 이유 |
|------|------|------|
| 1 | **Vault 컨텍스트 정비** (3번) | AI가 맥락 없이 작업하는 게 가장 비효율. 비용 0 |
| 2 | **자동화 스케줄러** (2번) | 서버에 올려두면 이후 매일 공짜로 distill 돌아감 |
| 3 | **Notion 마이그레이션** (1번) | 시간 걸리지만 vault 가치를 즉시 높임 |
| 4 | **task_type 변경** (4번) | 품질 개선, 변경은 단순하지만 영향 확인 필요 |
| 5 | **Decisions 승격** (5번) | 5분짜리, 아무 때나 |
